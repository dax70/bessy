

function selectSeats(state = [], action) {
  switch (action.type) {
    case 'SELECT_SEAT':

      break;
    case 'DESELECT_SEAT'
      break;
    default:
      return state;
  }

  return state;
}
